<h2>アーティスト新規登録</h2>

<%= form_for(@artist) do |f| %>

  <div class="field">
    <%= f.label :artist_name, "アーティスト名" %>
    <%= f.text_field :artist_name %>
  </div>

  <div class="actions">
    <%= f.submit "登録" %>
  </div>
<% end %>
