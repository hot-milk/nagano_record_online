<h2>アーティスト編集</h2>

<%= form_for(@artist) do |f| %>

  <div class="field">
    <%= f.label :artist_name, "レーベル名" %>
    <%= f.text_field :artist_name %>
  </div>

  <div class="actions">
    <%= f.submit "更新" %>
  </div>
<% end %>