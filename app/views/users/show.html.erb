<div class="container">
	<div class="row my-page-top">
		<section class="col-xs-12">
			<h3>マイページ</h3>
			<div class="row">
				<div class="col-xs-7 text-left">
					<div class="row">
						<div class="col-xs-3">
							<p><strong>ユーザー情報</strong></p>
						</div>
						<div class="col-xs-9">
							<%= form_for(@user) do |f| %>
								<p style="display:inline;">氏名：</p><%= @user.last_name %><%= @user.first_name %><br>
								<p style="display:inline;">住所：</p><%= @user.address %><br>
								<p style="display:inline;">電話番号：</p><%= @user.phone %><br>
								<p style="display:inline;">メール：</p><%= @user.email %><br>
							<% end %>
						</div>
					</div>
				</div>

				<div class="col-xs-5 text-right">
					<div class="row">
						<div class="col-xs-6">
							<%= link_to "ユーザー情報の変更はこちら", edit_user_path(@user), class: "" %>
						</div>
						<div class="col-xs-4">
							<%= link_to '退会', user_path(@user.id), method: :delete, "data-confirm" => "本当に退会しますか？", class: "btn-sm destroy_user_#{@user.id}" %>
						</div>
					</div>
				</div>
			</div>
		</section>
	</div>
<!-- 購入履歴 -->
	<div class="row">
		<div class="col-xs-10 col-xs-offset-1">
			<div class="panel panel-default">
				<div class="panel-heading">
					<h5 class="panel-title"><strong>購入履歴</strong></h5>
				</div>
				<% @orders.each do |order| %>
				<div class="panel panel-default">
				    <div class="panel-body">
						<table class="table table-condensed">
							<thead>
							<tr>
								<th>受取人</th>
								<th>購入日</th>
								<th>配送先</th>
							</tr>
						    </thead>
						    <tbody>
							<tr>
								<td><%= order.order_ship_last_name %><%= order.order_ship_first_name %></td>
								<td><%= order.created_at.strftime("%Y-%m-%d") %></td>
								<td><%= order.order_ship_post_code %><%= order.order_ship_address %></td>
							</tr>
						    </tbody>
						</table>
						<table class="table table-condensed table-striped table-hover">
							<thead>
							    <tr>
							    	<th>購入内容</th>
							    	<th>数量</th>
							    	<th>配送ステータス</th>
							    </tr>
							</thead>
							    <% order.order_items.each do |order_item| %>
							        <tbody>
										<tr >
											<td style="width: 300px"><%= order_item.product.product_name %></td>
											<td ><%= order_item.order_item_number %></td>
											<td ><%= order.ship_status %></td>
										</tr>
									</tbody>
							    <% end %>
						</table>
				    </div>
				</div>
				<% end %>
			</div>
		</div>
		<%= paginate @orders %>
	</div>
</div>





















