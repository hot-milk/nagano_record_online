<h2>マイページ</h2>
<h3>ユーザー情報</h3>

<% @user.each do |user| %>
<table class="table table-hover">
	<tr>
		<th>氏名</th>
		<td><%= user.last_name %> <%= user.first_name %>様</td>
	</tr>
	<tr>
		<th>住所</th>
		<td><%= user.postcpde %></td>
	</tr>
	<tr>
		<th></th>
		<td><%= user.address %></td>
	</tr>
	<tr>
		<th>電話番号</th>
		<td><%= user.phone %></td>
	</tr>
	<tr>
		<th>メールアドレス</th>
		<td><%= user.email %></td>
	</tr>
</table>

<% end %>

<%= link_to "個人情報の変更はこちら", edit_user_path(user.id), class: "btn btn-default btn-block" %>

<%= link_to "退会はこちら", user_registration_path(user.id), method: :delete, data: { confirm: ‘本当に退会してもよろしいですか?’ }, class: "btn btn-default btn-block" %>

<h3>購入履歴</h3>
<% @order.each do |order| %>
<table class="table table-hover">

